"""
Embedding logic.

WILL CONTAIN (Phase 1):
- embed(inputs): call ollama.embed(inputs), assert each vector length == EMBEDDING_DIM,
  return EmbedResponse { model, dim, embeddings }.

NO LOGIC YET — placeholder.
"""
